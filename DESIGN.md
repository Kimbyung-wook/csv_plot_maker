# CSV Plot Maker — 설계 및 요구사항

CSV 데이터를 불러와 여러 개의 subplot으로 구성된 그래프를 그리는 데스크톱 분석 도구다. 아래는 지금까지 정리된 요구사항과 설계 배경을 기능별로 묶어 정리한 것이다.

> 이 문서는 "지금 앱이 어떻게 동작해야 하는가"를 기능 단위로 정리한 요구사항 문서다. 날짜별 변경 이력이나 코드 수준의 구현 설명은 담지 않는다(필요하면 git 커밋 로그를 참고). 새 기능을 추가하거나 동작을 바꿀 때는 해당 항목을 찾아 최신 동작 기준으로 고쳐 쓰고, "언제 무엇을 고쳤다"는 식으로 덧붙이지 않는다.

사용 방법(실제 조작법) 안내는 [howtouse.md](howtouse.md)를 참고한다.

---

## 1. 핵심 목표

1. CSV 파일을 불러와 여러 개의 subplot으로 구성된 그래프를 그리는 데스크톱 GUI 분석 도구
2. 같은 데이터(컬럼)를 하나 이상의 시리즈·subplot에 중복해서 사용 가능
3. 사용자가 rows × cols 형태로 subplot 그리드를 자유롭게 구성
4. 시리즈(그래프 선)별 스타일(색상/선모양/마커/굵기)을 재시작 없이 즉시 변경
5. subplot별로 X/Y축 라벨을 직접 입력
6. subplot별로 이중 Y축(주축/보조축) 지원
7. 수십만~수백만 행 규모의 CSV도 빠르게 불러오고 매끄럽게 그릴 수 있어야 함

---

## 2. 화면 구성과 기본 조작

### 2.1 전체 레이아웃

- 좌측 도크: CSV 열기와 컬럼 목록
- 중앙: subplot 그리드가 그려지는 캔버스
- 우측 도크: 그리드 크기 설정 → 활성 subplot의 X축/시리즈 목록 → (시리즈를 선택했을 때만 나타나는) 스타일 편집 영역
- 창 제목에 현재 버전과 빌드일자가 항상 표시된다

### 2.2 데이터(컬럼) 선택

- CSV를 불러오면 실제 컬럼 외에 `Sequential`(1부터 시작하는 일련번호) 컬럼이 목록 맨 위에 자동으로 추가된다 — 쓸만한 X축 후보가 없을 때의 안전한 대안이다
- 컬럼이 많을 때는 검색 팝업으로 원하는 컬럼을 빠르게 찾을 수 있다
- 여러 컬럼을 범위 선택 또는 개별 선택으로 한 번에 고른 뒤, 원하는 subplot으로 한 번에 드래그해서 추가할 수 있다
- subplot의 시리즈 목록에서도 같은 방식으로 여러 시리즈를 선택해 한 번에 삭제하거나 다른 subplot으로 옮길 수 있다 — 이미 다른 subplot에 있는 데이터를 새 subplot 위로 드래그하면 "복사"가 아니라 "이동"으로 처리된다(원래 있던 곳에서는 사라지고 스타일은 그대로 유지됨). 다만 같은 데이터가 이미 여러 subplot에 흩어져 있다면 어디서 옮겨야 할지 모호하므로 그 경우는 이동시키지 않고 새로 추가만 한다

### 2.3 상호작용

- 그래프 영역의 subplot을 클릭하면 그 subplot이 활성화되고, 우측 패널이 그 설정을 보여준다
- 시리즈를 선택하면 스타일 편집 영역이 나타나고, 선택을 해제하면 다시 숨겨진다
- X/Y축 라벨은 그래프의 해당 축을 더블클릭하면 입력창이 뜬다
- "모든 subplot의 X축을 함께 확대·축소" 옵션을 켜면, 한 subplot에서 팬/줌으로 X축 범위를 바꿀 때 다른 모든 subplot의 X축도 같이 맞춰진다(Y축은 각자 유지)

### 2.4 CSV 로딩 중 상태 표시

- 큰 CSV는 컬럼 목록이 먼저 뜨고, 실제 데이터 파싱은 별도로 진행된다. 파싱이 끝나기 전에 데이터를 추가하려 하면 아직 준비되지 않았다는 안내가 표시된다
- 로딩 중에는 진행 상태를 알리는 팝업이 뜨고, 끝나면 자동으로 사라진다. 파싱 자체를 중간에 멈출 수는 없으므로 별도의 취소 버튼은 제공하지 않는다

### 2.5 이중 Y축

- subplot은 기본적으로 하나의(주) Y축만 사용한다
- 어떤 시리즈를 보조축으로 지정하면, 그 subplot에만 우측에 보조 Y축이 나타난다. 보조축을 쓰는 시리즈가 하나도 없으면 우측 축은 다시 숨겨진다

### 2.6 범례(Legend)

- 표시/숨김을 토글할 수 있으며 기본값은 표시다. subplot에 시리즈가 하나도 없으면 이 설정과 무관하게 자동으로 숨겨진다
- 배경은 완전히 불투명하게 채워져, 겹치는 격자선이나 그래프 선이 범례 뒤로 비쳐 보이지 않는다
- 글자 크기를 네 단계(Tiny/Small/Medium/Large)로 고를 수 있고, 그래프 전체에 함께 적용된다. 글자 크기가 작아질수록 각 항목의 아이콘과 줄 간격도 함께 줄어들어 여러 개를 한 화면에 촘촘히 보기 좋다

### 2.7 subplot 정렬

- 같은 그리드 안의 subplot들은 Y축 눈금 값의 자릿수가 서로 달라도 좌측 축 폭이 모두 동일하게 맞춰져, 그래프 영역의 왼쪽 경계가 세로로 가지런히 정렬된다
- 여러 열(column)로 구성된 그리드에서도 각 subplot의 폭이 고르게 유지된다 — 특정 subplot만 이중 Y축을 쓰거나 축 제목이 길다고 해서 그 열만 좁아지거나 넓어지지 않는다
- subplot을 세로로 여러 개 쌓았을 때 촘촘해서 Y축 눈금이 너무 적게 표시되는 문제가 없도록 눈금 밀도를 넉넉하게 유지한다

### 2.8 테마

- Light / Dark / System 모드 중에서 고를 수 있으며, 전환 시 배경·전경·축·범례 색상이 모두 함께 바뀐다

### 2.9 라이선스 안내

- Info 메뉴에서 앱 버전/빌드일자와 함께, 함께 배포되는 오픈소스 라이브러리들의 라이선스 정보 및 상업적 사용 관련 안내를 확인할 수 있다

### 2.10 저장/불러오기 진입점

- File 메뉴에서 현재 subplot 구성을 저장하거나, 저장된 구성을 다시 불러올 수 있다(자세한 내용은 [5장](#5-저장불러오기와-환경-설정) 참고)
- CSV나 저장된 구성을 새로 불러오면, 데이터에 맞춰 X·Y축 범위가 자동으로 맞춰진다

---

## 3. 데이터 불러오기와 전처리

### 3.1 CSV 로딩

- 컬럼 목록은 파일 전체를 다 읽지 않고도 즉시 표시된다
- 실제 데이터 파싱은 정상적인 CSV를 빠르게 처리하는 방식을 우선 시도하고, 까다로운 파일(앞부분과 뒷부분의 값 형태가 다른 컬럼 등)은 더 꼼꼼한 방식으로 자동 재시도하며, 그래도 문제가 있으면 문제 있는 값만 비워두고 나머지는 정상적으로 불러온다
- 다른 CSV를 열면 이전에 열려 있던 CSV의 subplot 구성은 초기화된다(컬럼 구조가 다른 파일 간 충돌을 막기 위함). 단, 같은 이름으로 저장해둔 구성이 있으면 자동으로 불러온다
- 각 행의 마지막 값이 종종 비어 있어 쉼표가 생략된 CSV도 정상적으로 불러와 빈 값은 결측으로 처리한다
- `HH:MM:SS.fff` 형식의 시간 문자열 컬럼은 자동으로 초 단위 숫자로 변환되어 그래프에 사용할 수 있다
- 어쩌다 한 번씩만 값이 채워지는 컬럼(예: 주기적으로만 갱신되는 상태값)이 문자열로 잘못 인식되어 그래프 후보에서 통째로 빠지는 일이 없도록, 값이 실제로는 숫자인지 다시 한 번 확인해서 복구한다
- 저사양 환경에서 큰 CSV(수 GB급)를 열 때 메모리 부족으로 컴퓨터가 멈추는 일이 없도록, 그래프에 쓸 수 없는 문자열 컬럼은 애초에 메모리에 올리지 않고, 파일 크기가 가용 메모리보다 훨씬 크면 미리 경고한다

### 3.2 컬럼 이름 정리 (Header Trimming)

- 모든 컬럼에 공통으로 붙은 접두/접미사(메시지 네임스페이스, 필드 표시자 등)를 지정한 키워드 목록에 따라 일괄 제거해 이름을 읽기 쉽게 만들 수 있다
- 키워드는 사용자가 직접 추가/삭제/전체삭제로 관리하며, 정리 결과 이름이 서로 겹치면 자동으로 번호를 붙여 구분한다
- 이 목록은 프로그램이 마음대로 저장하지 않는다 — 파일로 내보내거나 불러오는 것은 항상 사용자의 명시적인 동작으로만 이루어진다. 다만 실행 파일과 같은 폴더에 정해진 이름의 파일을 미리 두면, 시작할 때 그 파일을 한 번 읽어와 기본값으로 사용한다

---

## 4. 그래프/시리즈 기능

### 4.1 시리즈 스타일

- 시리즈별로 색상, 선 스타일(실선/파선/점선/파선점선/**없음**), 마커 종류, 선 굵기를 각각 독립적으로 설정할 수 있다
- 선 스타일을 "없음"으로 두면 선 없이 마커만 찍혀 산점도처럼 보인다
- 스타일 변경은 즉시 반영되며, 데이터가 아무리 많아도 지연이 없다
- 값이 드문(대부분 결측인) 컬럼을 그래프에 추가하면 자동으로 점(Dot) 마커만 찍히고 선은 그리지 않는 스타일이 기본 적용된다 — 값이 거의 없는데 선으로 이어버리면 실제로는 멀리 떨어진 시점을 매끄럽게 연결된 것처럼 오해할 수 있기 때문이다
- 마커가 설정된 시리즈는 화면을 아무리 축소해도 개별 값이 사라지지 않고 그대로 보인다(값이 드문 데이터일수록 이 점이 중요하다)

### 4.2 이중 Y축

- 시리즈별로 주축/보조축을 지정할 수 있고, 두 축은 서로 독립적으로 확대·축소된다

### 4.3 X축 설정

- subplot마다 X축에 사용할 컬럼을 선택할 수 있으며, 기본값은 `timestamp` 컬럼(없으면 `Sequential`)이다
- X축 값 전체에 더하거나 뺄 오프셋을 설정할 수 있어, 시간 데이터를 원하는 시점 기준으로 옮겨볼 수 있다. 버튼 한 번으로 "현재 데이터의 시작을 0으로" 맞출 수도 있다
- 활성 subplot에 맞춰둔 X축 컬럼과 오프셋을 그리드의 모든 subplot에 한 번에 복사하는 기능이 있다
- 그리드의 모든 subplot이 완전히 같은 X축 데이터(같은 컬럼, 같은 오프셋)를 사용하면, 맨 아래 줄의 subplot에만 X축 제목이 남고 나머지는 자동으로 비워져 그만큼 각 subplot의 그래프 영역이 넓어진다. 어느 한 곳이라도 X축이 달라지면 즉시 모든 subplot에 제목이 다시 표시된다

### 4.4 데이터 무결성

- `Sequential` 컬럼은 CSV 데이터를 건드리지 않는 가상의 일련번호 컬럼으로, 시간/타임스탬프 컬럼이 없거나 못 쓸 때의 안전한 대안이다

---

## 5. 저장·불러오기와 환경 설정

### 5.1 레이아웃 저장/불러오기

- 현재 CSV 기준의 정해진 경로에 저장하거나, 원하는 파일명으로 저장할 수 있다
- 저장된 구성을 다시 불러오거나, 임의의 저장 파일을 선택해 지금 열려 있는 CSV에 적용할 수 있다 — 저장 파일은 컬럼을 이름으로만 참조하므로 원래 저장했던 CSV가 아닌 다른 CSV에도 재사용할 수 있으며, 지금 CSV에 없는 컬럼을 참조하는 시리즈는 자동으로 걸러진다
- 저장되는 내용: 그리드 크기, subplot별 X축 컬럼/오프셋/라벨/범례 표시 여부/시리즈 목록과 스타일, 사용자가 확대·축소해 둔 Y축(주축·보조축) 범위, 범례 글자 크기, "모든 subplot의 X축을 함께 확대·축소" 설정
- 예전 버전에서 저장한 파일도 새 항목이 없다는 이유로 실패하지 않고, 빠진 항목은 기본값으로 안전하게 채워진다

### 5.2 기타 편의 기능

- 활성 subplot만 초기화하거나, 전체 subplot을 한 번에 초기화할 수 있다
- 컬럼이 매우 많은 CSV에서 원하는 컬럼을 빠르게 찾는 검색 기능이 있다
- 여러 컬럼을 한 번의 드래그로 한 subplot에 동시에 추가할 수 있다
- 목록에서 시리즈를 선택한 뒤 삭제 키로 바로 제거할 수 있다

---

## 부록. 구현 참고 (개발자용 요약)

**기술 스택**: Python 기반 데스크톱 앱으로, 화면은 Qt(PySide6), 그래프 렌더링은 pyqtgraph, CSV 파싱은 polars를 사용한다. 대용량 데이터를 다루면서도 스타일 변경이 매번 즉시 반영되어야 한다는 요구사항 때문에, 스타일을 바꿀 때마다 화면 전체를 다시 그려야 하는 matplotlib 대신 이 조합을 선택했다.

**구조**: CSV 파싱과 화면에 뜨는 데이터를 담당하는 부분, subplot·시리즈 등 저장 가능한 상태를 표현하는 부분, 화면(창/패널/다이얼로그)을 구성하는 부분, 실제 그래프 렌더링을 담당하는 부분으로 나뉘어 있다. 데이터/상태 관련 부분은 화면 프레임워크에 의존하지 않아 별도로 테스트하기 쉽게 만들어졌다. X축 컬럼은 시리즈가 아니라 subplot 단위로 공유되고, 스타일이나 라벨을 바꿀 때는 화면 전체가 아니라 바뀐 부분만 다시 그리도록 되어 있다.

**성능 전략**: 컬럼 목록은 파일 전체를 읽지 않고 구조만 조회해 즉시 보여준다. 실제 데이터는 정상 케이스를 빠르게 처리하는 방식을 우선 시도하고, 까다로운 파일만 더 꼼꼼한 방식으로 재시도해 속도와 안정성을 함께 확보한다. 그래프는 화면에 보이는 범위만 실제로 그리도록 최적화되어 있어 확대/축소가 매끄럽고, 마커로 개별 값을 보고 싶을 때만 이 최적화를 끄고 원본 그대로 그린다. 대용량 CSV를 열 때는 메모리 사용량을 최소화하고, 필요 이상으로 부족한 환경에서는 미리 경고한다.

**검증 방법**: 데이터 처리와 상태 관리 로직은 자동화된 테스트로 검증한다. 화면 렌더링, 정렬, 테마 전환처럼 눈으로 봐야 하는 부분은 실제로 띄워보고 확인한다. 대용량 CSV는 실제 크기의 합성 데이터로 로딩 시간과 조작 반응성을 측정해 확인한다.
